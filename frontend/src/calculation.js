export function sumEVs({ statRows }) {
    let sum = 0;
    for (const stat of statRows) {
        sum += stat.ev;
    }
    return sum;
}

export function calcHP({}) {
    
}

export function calcStat({}){

}